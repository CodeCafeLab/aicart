import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function NewShootPage() {
  return (
    <div>
      <h1 className="font-headline text-3xl font-bold tracking-tight">Start New Shoot</h1>
      <Card className="mt-4">
        <CardHeader>
          <CardTitle>New Shoot</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is a placeholder for the new shoot page.</p>
        </CardContent>
      </Card>
    </div>
  );
}

    