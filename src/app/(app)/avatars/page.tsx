import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function AvatarsPage() {
  return (
    <div>
      <h1 className="font-headline text-3xl font-bold tracking-tight">AI Models / Avatars</h1>
      <Card className="mt-4">
        <CardHeader>
          <CardTitle>AI Models / Avatars</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is a placeholder for the AI models / avatars page.</p>
        </CardContent>
      </Card>
    </div>
  );
}

    